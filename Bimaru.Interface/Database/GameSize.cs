namespace Bimaru.Interface.Database
{
    /// <summary>
    /// Possible game sizes (e.g. small)
    /// </summary>
    public enum GameSize
    {
        SMALL = 10,
        MIDDLE = 20,
        LARGE = 30
    }
}
